Noggit SDL 1.3 changelog

Done:

- Add app Icon
- You can now load BGs
- Add load all tileset function to texture pallet
- Change rows and cols direction and count
- Add big preview of selected texture
- Discard the old current texture window
- Fix texture pallet size to fit names
- Do Rel with debug now
- ALT + 1 till 5 set now the texture paint opercety to 100,75,50,25 and 0 %
- Space + Maus = Speed and Pressure
- Log file caonfig file hints
- Link to Manual wiki
- Load MPQs now in read only
- WMO culing Work now better
- Add ground flatten&blur speed
- UID Fix
- Clean out test and deprecated menu functions
- Maptile display on minimap
- Auto size,tile and rotation works now also if you have copy model size and rotation activated.
- In Holes mode you can now alos edit the full chunk if you hold down the ALT key during edit.
- Water save

ToDo:
- Water fix for custom added water
- better Vertics Rendering ( Hanfer )
- water delete all
- Water full create at level x perhaps just over slider with realtime rendering.

bugs

- Resize bug. After the load of a map and the resice of the window the selection do not work.
- Some WMOs crash noggit on render call. Like in Mulgot near start area.
- Some models render not or wrong. Perhaps 2 side force. Some mushrooms for example
- Fish model render wrong.
- Holes tool sometimes make hols where it likes. Its because the old selectin. But hols work not with new one.
- Bigger models (Like stormwind) should mark and load all adts they are located on after insert, move or delete.
- After you have saved the selection works not. You have to cloase noggit and reload.
- There is no unloading of ADTs. We should think abut and implement
- WMO culling dont work 100%. On big models you have often parts that disapeare.
- If you make a model copy to clipboard, delete it then and past then noggit crash.